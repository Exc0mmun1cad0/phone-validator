name: Github Actions Demo

run-name: ${{ github.actor }} is testing out Github Actions 🚀 (cringe emoji)

on:
  workflow_dispatch:

jobs:
    Explore-Github-Actions:
        runs-on: ubuntu-latest
        steps:
          - run: echo "The job was automatically triggered by a ${{ github.event_name }} event."

          - run: echo "This job is now running on a ${{ runner.os }} server hosted by GitHub."

          - run: echo "The name of branch is ${{ github.ref }} and your repository is ${{ github.repository }}."

          - name: Check out repository code
            uses: actions/checkout@v4

          - run: echo "The ${{ github.repository }} repositroy has been cloned to the runner."

          - run: echo "The workflow is now ready to test your code on the runner."

          - name: "List files in the repository"
            run: |
              ls ${{ github.workspace }}
              pwd
            
          - run: echo "This job status is ${{ job.status }}"